- 小组编号：10
- 小组成员：杨家兴、包阳、易炜涵、孟昶羽
- 认领类型：主算子
- 算子名称：moe_expand_to_fused
- 候选清单状态：待迁移
- 候选清单难度：中
- 源文件路径：`tile_kernels/moe/expand_to_fused_kernel.py`
- 源提交 SHA：
- 算子入口：`expand_to_fused(x: torch.Tensor, token_topk_to_pos: torch.Tensor, pos_to_expert: torch.Tensor) -> torch.Tensor`；量化变体 `expand_to_fused_with_sf(x: QuantTensor, num_per_channels: int, token_topk_to_pos: torch.Tensor, pos_to_expert: torch.Tensor, use_tma_aligned_col_major_sf: bool = False) -> tuple[torch.Tensor, torch.Tensor]`
- 核心 Kernel：`get_expand_to_fused_kernel(hidden, num_topk, num_per_channels, use_tma_aligned_col_major_sf, use_packed_ue8m0, x_dtype, sf_dtype)` 生成的 `expand_to_fused_kernel`
- 主要工作负载：连续 FP16/BF16（量化路径为 FP8 `e4m3` / FP4 `e2m1`）激活矩阵 `x[num_tokens, hidden]`，按 `token_topk_to_pos`（INT32，`[num_tokens, num_topk]`）把每个 token 散射复制到最多 `num_topk` 个 expert-major 位置，输出 `expanded_x[num_expanded_tokens, hidden]`；`pos < 0` 的 topk 槽跳过，`pos_to_expert < 0` 的空位清零。量化路径同步散射 scale factor（`num_per_channels ∈ {32, 128}`），支持 row-major 与 TMA-aligned col-major sf 布局，以及 `packed_ue8m0`（INT32 打包 4 个 UE8M0 指数、强制 col-major）。纯访存型负载（`flops = 0`），Manifest 已登记工作负载为 deepseek-v3-decode（`x=[32, 7168]`、`num_topk=8`、`num_expanded_tokens=256`）与 deepseek-v3-prefill（`x=[512, 7168]`、`num_topk=8`、`num_expanded_tokens=4096`）。
- 预期泛化、C500 适配或优化目标：按 TileOPs 的 Manifest → Test → Op/Kernel → Benchmark 规范完成迁移。`tileops/manifest/moe.yaml` 中已存在 `MoeExpandToFusedFwdOp` 条目且 `status: spec-only`，因此本课题的交付物是补齐 `tileops/kernels/moe/expand_to_fused.py`、`tileops/ops/moe/expand_to_fused.py`、`tests/ops/test_moe_expand_to_fused.py`、`benchmarks/ops/bench_moe_expand_to_fused.py` 并按状态翻转碎片规则把 `status` 翻到 `implemented`，不修改现有 signature / roofline / shape_rules。现有 signature 只覆盖非量化路径（`x: float16 | bfloat16`，输出 `expanded_x`），FP8/FP4 + scale-factor 变体不在当前 spec 内，若要一并覆盖需先提独立的 manifest-only PR 扩展 signature，再在实现 PR 中落地。泛化目标：支持动态 `num_tokens` / `num_expanded_tokens`、任意 `hidden`（非 64 对齐场景）、不同 `num_topk`，以及 `num_tokens == 0` 与全负 `pos_to_expert` 的退化输入。C500 适配点：验证 `T.dynamic` 动态符号（`num_tokens` / `num_expanded_tokens` / `sf_stride`）、`T.StridedTensor` 变步长 sf 张量、`T.thread_return()` 提前退出、`T.assume()` 边界假设，以及 `TL_DISABLE_WARP_SPECIALIZED` pass config 在 MACA 后端的可用性；核对 `num_threads = 64` 与 `align(hidden, 64)` 在 C500 warp 宽度下的向量化访存宽度是否仍是最优。优化目标：该算子为带宽受限，roofline 字节数为 `(T * H + P * H) * elem_bytes + T * K * 4 + P * 4`，重点评估向量化位宽、`x` 在 `num_topk` 次散射间的寄存器复用（避免重复读全局内存）、`num_blocks = max(num_tokens, num_expanded_tokens)` 下清零分支与散射分支的特化、小 token 场景（decode，`num_tokens = 32`）的并行度不足问题，以及每 block 处理多 token 的方案；用 mcProfiler 量化访存效率，报告绝对延迟、独立基线、加速比与 Roofline 带宽利用率。
- C500 初步验证情况：尚未验证
- 可能包含、依赖或共享实现的其他算子：`tile_kernels/utils.py`：`align`、`ceil_div`。`tile_kernels/quant/types.py`：`QuantTensor`（`tuple[torch.Tensor, torch.Tensor]`）；`tile_kernels/quant/common.py`：`BaseCastConfig` 的 `sf_block` / `use_tma_aligned_col_major_sf` / `use_packed_ue8m0` 语义与 `get_best_vectorize_size`。上游 `tile_kernels/moe/get_fused_mapping_kernel.py` 生产本算子消费的 `token_topk_to_pos` / `pos_to_expert` 映射表；`tile_kernels/moe/reduce_fused_kernel.py` 是本算子的反向加权 gather，两者共享 expert-major 布局约定。TileOPs 现有 `MoePermuteNopadFwdOp` / `MoePermuteAlignFwdOp` / `MoeUnpermuteFwdOp` 在 permute/unpermute 语义上部分重叠，可复用其测试构造与 benchmark 结构，但不修改其现有接口与语义。
- 与已有认领课题的区别：本课题只负责 expand（scatter）这一步：`moe_get_fused_mapping` 负责生成映射表，`moe_reduce_fused` 负责反向加权归约，三者是同一条 fused MoE 路径上的不同阶段，接口与输出均不相同。与 TileOPs 现有 `MoePermuteNopadFwdOp` 的区别在于：后者从 `topk_ids` 自行计算 permute 顺序并额外输出 `true_offsets` / `true_sizes` / `expert_first_token_offset` / `fwd_idx`，输出行数固定为 `num_tokens * top_k`；本算子消费外部预先算好的 `token_topk_to_pos` / `pos_to_expert`，输出行数由 `pos_to_expert` 长度决定，且支持负 pos 跳过、空位清零、量化 scale-factor 同步散射以及 col-major / packed-ue8m0 的 sf 布局，因此属于独立算子，不是对已有算子的重复认领。
- 是否确认该算子在候选清单中：是（候选清单 MoE Routing 分组，`moe_expand_to_fused`，待迁移，难度中）
- 是否确认该算子尚未迁入 `TileOPs-Metax`，或已被标注为可优化：是 —— `tileops/manifest/moe.yaml` 中已有 `MoeExpandToFusedFwdOp` 条目，但 `status: spec-only`，`source` 指向的 kernel / op / test / bench 四个文件均不存在，即已有 spec 未有实现。
- 是否发现其他小组已认领相同或实质重叠的课题：否
